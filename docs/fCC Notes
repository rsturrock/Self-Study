#Stand in Line 

function nextInLine(arr, item) {
  arr.push(item);
  var x = arr.shift(); // Your code here
  
  return x;  // Change this line


----------------------------------------------------------------------------------------------------------------------------------------
