#Questions

1)
  a) == checks for equality with coercion allowed.
  b) === checks for equality without coercion allowed.
  c) = checks for what?

Case C) is not a relational operator but an assignment operator.

-------------------------------------------------------------------------------------------------------------------------------------------

2) What are the 3 steps of compilation?
